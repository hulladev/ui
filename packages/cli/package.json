{
  "name": "cli",
  "private": true,
  "description": "Multi-platform low-code UI generator 🤖",
  "type": "module",
  "bin": "./dist/bin/index.js",
  "scripts": {
    "lint": "eslint --ext .ts --fix .",
    "build": "bunchee -m",
    "watch": "bunchee -w",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "is-unicode-supported": "^2.0.0",
    "picocolors": "^1.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "codemods": "workspace:*"
  }
}