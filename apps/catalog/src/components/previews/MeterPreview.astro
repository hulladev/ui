---
import FieldLabel from "@/ui/field/field-label.astro"
import FieldOutput from "@/ui/field/field-output.astro"
import FieldHeader from "@/ui/field/field-header.astro"
import Meter from "@/ui/meter/meter.astro"
import manifest from "../../../../../generated/ui.manifest.json"
import ComponentPreview from "../ComponentPreview.astro"

const meter = manifest.library.components.meter
const signals = [
  {
    detail: "18.4 GB of 24 GB",
    high: 21,
    label: "Storage",
    low: 12,
    max: 24,
    optimum: 8,
    value: 18.4,
  },
  {
    detail: "84 / 100",
    high: 100,
    label: "Release confidence",
    low: 60,
    max: 100,
    optimum: 100,
    value: 84,
  },
  {
    detail: "9 of 10",
    high: 7,
    label: "Open incidents",
    low: 3,
    max: 10,
    optimum: 0,
    value: 9,
  },
] as const
---

<ComponentPreview
  component="Meter"
  hint="Native measurement · semantic thresholds"
  propsLabel="Native range"
  requirePropsView={true}
>
  <div slot="variants" class="grid w-full gap-6">
    {
      signals.map(({ detail, high, label, low, max, optimum, value }, index) => (
        <div class="grid gap-2">
          <FieldHeader>
            <>
              <FieldLabel for={`meter-signal-${index}`}>{label}</FieldLabel>
              <FieldOutput>{detail}</FieldOutput>
            </>
          </FieldHeader>
          <Meter
            id={`meter-signal-${index}`}
            aria-label={label}
            value={value}
            min={0}
            max={max}
            low={low}
            high={high}
            optimum={optimum}
          >
            {detail}
          </Meter>
        </div>
      ))
    }
    <p class="text-xs text-muted-foreground">
      Default appearance. Native low, high, and optimum thresholds determine the success, warning,
      or danger fill.
    </p>
  </div>

  <div slot="props" class="prop-playground">
    <div class="prop-controls">
      <label class="prop-control">
        <span class="label">Value</span>
        <input type="range" min="0" max="100" value="72" data-meter-value />
      </label>

      <label class="prop-control">
        <span class="label">Optimum</span>
        <select aria-label="Meter optimum" data-meter-optimum>
          <option value="100" selected>Higher is better</option>
          <option value="0">Lower is better</option>
        </select>
      </label>
    </div>

    <div class="playground-stage">
      <div class="grid w-72 max-w-full gap-2">
        <FieldHeader>
          <FieldLabel for="meter-playground">Signal quality</FieldLabel>
          <FieldOutput data-meter-output>72 / 100</FieldOutput>
        </FieldHeader>
        <Meter
          id="meter-playground"
          value={72}
          min={0}
          max={100}
          low={25}
          high={60}
          optimum={100}
          aria-label="Signal quality"
          data-playground-meter>72%</Meter
        >
      </div>
    </div>

    <code class="prop-readout" data-meter-readout aria-live="polite"
      >&lt;Meter value=&#123;72&#125; min=&#123;0&#125; max=&#123;100&#125; optimum=&#123;100&#125;
      /&gt;</code
    >
  </div>

  <dl slot="facts" class="facts">
    <div class="fact">
      <dt class="label">Available in</dt>
      <dd>{meter.frameworks.join(" · ")}</dd>
    </div>
    <div class="fact">
      <dt class="label">Native element</dt>
      <dd>meter · implicit meter role</dd>
    </div>
    <div class="fact">
      <dt class="label">Custom props</dt>
      <dd>none · native thresholds</dd>
    </div>
  </dl>
</ComponentPreview>
