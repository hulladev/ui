---
import { breadcrumbsPreviewSources } from "../../data/catalogPreviewSources"
import ComponentSource from "../ComponentSource.astro"
import BreadcrumbsPreview from "../previews/BreadcrumbsPreview.astro"

const usageSources = [
  {
    code: `---
import BreadcrumbEllipsis from "@/ui/breadcrumbs/breadcrumb-ellipsis.astro"
import BreadcrumbItem from "@/ui/breadcrumbs/breadcrumb-item.astro"
import Breadcrumbs from "@/ui/breadcrumbs/breadcrumbs.astro"
---

<Breadcrumbs>
  <BreadcrumbItem><a href="/projects">Projects</a></BreadcrumbItem>
  <BreadcrumbItem><BreadcrumbEllipsis /></BreadcrumbItem>
  <BreadcrumbItem><a href="/projects/atlas">Atlas</a></BreadcrumbItem>
  <BreadcrumbItem aria-current="page">Settings</BreadcrumbItem>
</Breadcrumbs>`,
    filename: "src/components/project-breadcrumbs.astro",
    framework: "Astro",
    language: "astro" as const,
  },
  {
    code: `import { BreadcrumbItem, Breadcrumbs, BreadcrumbEllipsis } from "@/ui/breadcrumbs"

export function ProjectBreadcrumbs() {
  return (
    <Breadcrumbs>
      <BreadcrumbItem>
        <a href="/projects">Projects</a>
      </BreadcrumbItem>
      <BreadcrumbItem>
        <BreadcrumbEllipsis />
      </BreadcrumbItem>
      <BreadcrumbItem>
        <a href="/projects/atlas">Atlas</a>
      </BreadcrumbItem>
      <BreadcrumbItem aria-current="page">Settings</BreadcrumbItem>
    </Breadcrumbs>
  )
}`,
    filename: "src/components/project-breadcrumbs.tsx",
    framework: "React",
    language: "tsx" as const,
  },
]
---

<section class="component-section" id="breadcrumbs" aria-labelledby="breadcrumbs-section-title">
  <div class="section-head">
    <div>
      <p class="eyebrow">Primitive / Navigation</p>
      <h2 id="breadcrumbs-section-title">Breadcrumbs</h2>
    </div>
    <span class="status">Two-part composition</span>
  </div>

  <article class="bench glass-panel" aria-label="Breadcrumbs component preview">
    <div class="workbench">
      <BreadcrumbsPreview />

      <ComponentSource
        component="Breadcrumbs"
        generatedFamilies={["breadcrumbs"]}
        previewSources={breadcrumbsPreviewSources}
        usageSources={usageSources}
      />
    </div>
  </article>
</section>

<script>
  document.addEventListener("change", (event) => {
    const control = (event.target as HTMLElement).closest<HTMLSelectElement>(
      "[data-breadcrumbs-composition]"
    )
    if (!control) return

    const preview = control.closest<HTMLElement>('[data-preview-component="breadcrumbs-preview"]')
    preview
      ?.querySelectorAll<HTMLElement>("[data-playground-breadcrumbs]")
      .forEach((breadcrumbs) => {
        breadcrumbs.hidden = breadcrumbs.dataset.composition !== control.value
      })

    const readout = preview?.querySelector<HTMLElement>("[data-breadcrumbs-readout]")
    if (readout) {
      const description = control.value === "collapsed" ? "…collapsed path…" : "…full path…"
      readout.textContent = `<Breadcrumbs>${description}</Breadcrumbs>`
    }
  })
</script>
